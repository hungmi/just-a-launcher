# tvhome

給 BenQ GV01（Android TV 14）的最小 launcher：一個 GridView 列出 LEANBACK_LAUNCHER 的 app，
加一格「設定」。沒有動畫、桌布、服務。目標常駐記憶體 < 40MB。

編譯走 GitHub Actions（`build.yml`），artifact `tvhome-apk`。細節見 `~/dotfiles/PROJECTOR.md`。
